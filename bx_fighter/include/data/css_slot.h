#pragma once

#include "bx.h"

// TODO: find a better way to display this
// NOTE: each entry can have up to 16 sub entries (colors)
extern CSSColors CSSSlotColors[MAX_CHARS];

extern CSSSlotInfo CSSInfo[MAX_CHARS];

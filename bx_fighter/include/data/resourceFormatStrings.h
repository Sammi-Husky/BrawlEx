#pragma once

#include "bx.h"

extern char *cmnFighterPath;
extern char *pacExtensionStr;
extern char *pcsExtensionStr;
extern char *darkStr;
extern char *fakeStr;
extern char *spyStr;
extern char *finalStr;
extern char *motionStr;
extern char *etcStr;
extern char *motionEtcStr;
extern char *entryStr;
extern char *resultStr;

// Resource Format Strings
static char *ResourceFormatStrings[] = {cmnFighterPath, pacExtensionStr, pcsExtensionStr, darkStr,
                                        fakeStr,        spyStr,          finalStr,        motionStr,
                                        etcStr,         motionEtcStr,    entryStr,        resultStr};